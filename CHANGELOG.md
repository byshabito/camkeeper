# Changelog

## v0.1.0

- Cross-site model profiles with multiple platform usernames
- Notes, tags, and social handles with smart URL parsing
- Auto-open detail view on saved model pages
- Add current platform to an existing profile
- Bulk select with merge and delete
- Local-only storage with JSON export/import
- Chrome and Firefox support (with library fallback for popup blockers)
