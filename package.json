{
  "name": "camkeeper",
  "private": true,
  "type": "module",
  "scripts": {
    "build:ext": "node scripts/build-extension.mjs",
    "dev:unpacked": "node scripts/build-unpacked.mjs --browser=chrome",
    "dev:unpacked:firefox": "node scripts/build-unpacked.mjs --browser=firefox",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "esbuild": "^0.27.3",
    "nostr-tools": "^2.23.1",
    "vitepress": "^1.6.4"
  }
}
